// 25. El Objeto Math

let resultado;

// PI
resultado = Math.PI;

// Redondeo
resultado = Math.round(2.5); // Redondea al entero más cercano
resultado = Math.round(2.4); // Redondea al entero más cercano

// Redondeo hacia arriba
resultado = Math.ceil(2.1); // Redondea hacia arriba
// Redondeo hacia abajo
resultado = Math.floor(2.9); // Redondea hacia abajo

// Raíz cuadrada
resultado = Math.sqrt(64); // Raíz cuadrada de 64
// Raíz cúbica
resultado = Math.cbrt(27); // Raíz cúbica de 27
// Potencia
resultado = Math.pow(2, 3); // 2 elevado a la potencia de 3

// Valor absoluto
resultado = Math.abs(-5); // Valor absoluto de -5
// Mínimo
resultado = Math.min(10, 20, 30); // Mínimo entre los números
// Máximo
resultado = Math.max(10, 20, 30); // Máximo entre los números

// Aleatorio
resultado = Math.random(); // Número aleatorio entre 0 y 1
// Aleatorio entre 0 y 100
resultado = Math.floor(Math.random() * 100); // Número aleatorio entre 0 y 100
